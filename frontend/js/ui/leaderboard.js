window.UIBoard={async render(){const d=document.createElement('div');d.innerHTML='<h2>🏆 BXH tiền</h2><div id="board" class="grid"></div>';const x=await API.get('/api/leaderboard');d.querySelector('#board').replaceChildren(...x.money.slice(0,10).map((u,i)=>{const c=document.createElement('div');c.className='card';c.textContent=`#${i+1} ${u.display_name||u.username||u.id} — ${formatNumber(u.value)} xu`;return c}));return d}}