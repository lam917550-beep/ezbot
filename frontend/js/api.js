let token=''; export function setToken(t){token=t;localStorage.setItem('casino_token',t)} export function getToken(){return token||localStorage.getItem('casino_token')||''} export async function api(path,opts={}){const headers={'Content-Type':'application/json',...(opts.headers||{})};const t=getToken();if(t)headers.Authorization='Bearer '+t;const r=await fetch(path,{...opts,headers});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||'API error');return data} export async function auth(initData){const data=await api('/api/auth',{method:'POST',body:JSON.stringify({initData})});setToken(data.token);return data.user}
