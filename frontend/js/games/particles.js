export function particleBurst(canvas,count=40){const c=canvas.getContext('2d');const pts=Array.from({length:count},()=>({x:canvas.width/2,y:canvas.height/2,vx:(Math.random()-.5)*8,vy:(Math.random()-.5)*8,a:1}));function f(){c.clearRect(0,0,canvas.width,canvas.height);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vy+=.12;p.a-=.02;c.globalAlpha=Math.max(0,p.a);c.fillRect(p.x,p.y,3,3)});if(pts.some(p=>p.a>0))requestAnimationFrame(f)}f()}
