export function detectTier(){const cores=navigator.hardwareConcurrency||2,ram=navigator.deviceMemory||2,save=navigator.connection?.saveData;return (save||cores<=2||ram<=2)?'LITE':(cores>=8&&ram>=8)?'ULTRA':'HIGH'}
export function startAdaptiveTier(onChange){let tier=detectTier(),bad=0,good=0,last=performance.now(),frames=0;function f(now){frames++;if(now-last>=1000){const fps=frames;frames=0;last=now;if(fps<24)bad++;else bad=0;if(fps>55)good++;else good=0;if(bad>=3&&tier!=='LITE'){tier=tier==='ULTRA'?'HIGH':'LITE';bad=0;onChange(tier)}else if(good>=8&&tier==='LITE'){tier='HIGH';good=0;onChange(tier)}requestAnimationFrame(f)}else requestAnimationFrame(f)}document.body.dataset.tier=tier;requestAnimationFrame(f);return()=>tier}
export function startFpsMonitor(onChange){let frames=0,last=performance.now();function f(now){frames++;if(now-last>=1000){onChange(frames);frames=0;last=now}requestAnimationFrame(f)}requestAnimationFrame(f)}
