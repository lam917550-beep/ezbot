const db=require('../../database/db'); const {nowSec}=require('../../utils/time'); module.exports={get:(id,season)=>db.prepare('SELECT * FROM season_pass WHERE user_id=? AND season=?').get(String(id),season),addXP:(id,season,xp)=>db.prepare('INSERT INTO season_pass(user_id,season,xp,tier) VALUES(?,?,?,?) ON CONFLICT(user_id,season) DO UPDATE SET xp=xp+excluded.xp,tier=CAST((xp+excluded.xp)/100 AS INTEGER)').run(String(id),season,xp,Math.floor(xp/100))};
