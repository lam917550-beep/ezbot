const db=require('../../database/db'); const C=require('../../config/constants'); let cache={time:0,data:null};
function rows(sql){return db.prepare(sql).all().map(r=>({...r,rank:r.rank||0}))}
function getAllLeaderboards(){if(cache.data&&Date.now()-cache.time<C.LEADERBOARD_TTL) return cache.data; const top=(col)=>db.prepare(`SELECT id,username,display_name,avatar,${col} AS value FROM users WHERE banned=0 AND deleted_at IS NULL ORDER BY ${col} DESC LIMIT 100`).all(); cache={time:Date.now(),data:{money:top('balance'),level:top('level'),bank:top('bank'),loginStreak:top('login_streak'),winStreak:top('max_win_streak'),totalWins:top('total_wins')}}; return cache.data}
module.exports={getTopMoney:l=>getAllLeaderboards().money.slice(0,l),getTopLevel:l=>getAllLeaderboards().level.slice(0,l),getTopBank:l=>getAllLeaderboards().bank.slice(0,l),getTopLoginStreak:l=>getAllLeaderboards().loginStreak.slice(0,l),getTopWinStreak:l=>getAllLeaderboards().winStreak.slice(0,l),getTopTotalWins:l=>getAllLeaderboards().totalWins.slice(0,l),getAllLeaderboards,getUserRank:(id,type)=>{const arr=getAllLeaderboards()[type]||[]; const i=arr.findIndex(x=>x.id===String(id)); return {rank:i<0?null:i+1,value:i<0?0:arr[i].value}}};
